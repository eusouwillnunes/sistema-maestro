# Checklist tipo — Entrega genérica (fallback)

Aplicado quando o tipo do artefato não tem `tipo-{x}.md` específico.

- [ ] Seção `## Contexto` preenchida (não vazia)
- [ ] Seção `## Resultado` preenchida com produção do especialista
- [ ] Seção `## Fontes e wiki-links` preenchida ou explicitamente vazia ("Nenhuma fonte adicional além do contexto da tarefa.")
